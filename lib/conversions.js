//const convert = require('convert-units');

// function convert(unit, from, to) {
//     //example of base library to wrap...need to add a dict of terms (i.e. miles, mile, m all should map to mi....and need to add km, for some reason it doesnt go higher than m) 
//     return convert(unit).from(from).to(to)
// }